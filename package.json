{
  "name": "todo-task",
  "version": "1.0.0",
  "description": "todo task",
  "main": "index.html",
  "scripts": {
    "build": "npx parcel build ./src/*.html --no-source-maps --public-url . --no-cache",
    "start": "npx parcel ./src/*.html"
  },
  "author": "Alaa Abbas",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "sass": "^1.52.3"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.5",
    "bootstrap": "^5.1.3"
  }
}
